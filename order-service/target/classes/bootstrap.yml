spring:
  cloud:
    nacos:
      server-addr: 10.211.55.7
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yaml
          - data-id: order-service.yaml
            refresh: true

